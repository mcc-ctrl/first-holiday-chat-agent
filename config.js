const config = {
    db: {
        /* don't expose password or any sensitive info, done only for demo */
        host: "localhost",
        user: "Matt",
        password: "Projectx=23",
        database: "holiday_packages",
    },
    listPerPage: '',
};
module.exports = config;